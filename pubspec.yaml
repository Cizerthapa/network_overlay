name: assistive_touch_overlay
description: >-
  Assistive Touch-style draggable floating bubble overlay for Flutter
  (snap-to-edge + optional pulse) with an optional Dio API inspector.
version: 0.0.2
repository: https://github.com/Cizerthapa/network_overlay

topics:
  - ui
  - overlay
  - debugging
  - network
  - dio

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

# Co-authored and inspired by Safal Shrestha
